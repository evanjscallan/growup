// This file can be replaced during build by using the `fileReplacements` array.
// When plant for production, this file is replaced with `environment.prod.ts`.

export const environment = {
  production: false,
};
